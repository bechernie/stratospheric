{
  "name": "deployment-sequencer-lambda",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dist": "mkdir -p dist && cp src/index.js dist/index.js && npm run zip",
    "zip": "cp -r node_modules dist/node_modules && cd dist && zip -r lambda.zip index.js node_modules",
    "clean": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-sqs": "3.631.0",
    "axios": "1.7.4"
  }
}
